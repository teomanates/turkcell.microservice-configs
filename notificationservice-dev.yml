spring:
  cloud:
    discovery:
      enabled: true
    stream:
      kafka:
        binder:
          brokers: kafka:9092

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8888/eureka/